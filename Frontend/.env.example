# Set this if your frontend is deployed separately from the backend.
# If frontend and backend are served from the same host, the app should use the backend origin.
VITE_API_URL=https://chatify-backend.onrender.com/api
