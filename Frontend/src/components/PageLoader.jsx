const PageLoader = () => {
  return (
    <div className="flex items-center justify-center min-h-screen w-full bg-slate-900 border-none outline-none">
      <span className="text-cyan-400 text-lg font-bold">Loading Chatify...</span>
    </div>
  );
};

export default PageLoader;
